name: tf_train
channels:
  - conda-forge
dependencies:
# Basics
  - python=3.*
  - cudatoolkit=11.2
  - cudnn=8.1.0
  # Note - need to add $CONDA_PREFIX/lib/ to end of LD_LIBRARY_PATH
  - pip
  - pip:
  # For bilinear interpolation
    - tensorflow

